import {useContext} from 'react'
import GatewayContext from '../context/GatewayContext'

// eslint-disable-next-line import/no-anonymous-default-export
export default () => useContext(GatewayContext);
