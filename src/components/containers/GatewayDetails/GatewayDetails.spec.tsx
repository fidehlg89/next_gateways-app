import React from "react";
import { render } from "@testing-library/react";
import GatewayDetails from "../GatewayDetails";

describe("Gateway Details", () => {
  it.skip("renders GatewayDetails component", () => {

  });
});
